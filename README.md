# Socket.io Practice

## Setup
> Runs the `npm install` and `bower install` first

## Usage

Open 2 terminal, in one run:
```bash
$ node server
```

in two run:
```bash
$ grunt serve
```

> Go to `localhost:9000`
> Click `Test` Button